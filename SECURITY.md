# 安全说明

## 安全边界

本项目只面向已经安装天锐绿盾客户端、且当前 Windows 用户本来就有权发起“申请解密”的环境。它调用本机官方组件并保留原有审批链，不绕过权限、策略或审批，不修改绿盾驱动、数据库与文件密文。

MCP 服务只监听当前 Windows 用户可访问的本机命名管道，并只处理 Agent 当前任务中由用户明确指定的绝对路径。项目不会自动扫描整台电脑，也不会把“申请已发送”视为“解密已经成功”。

## 报告安全问题

如果发现可能扩大解密范围、绕过授权边界、泄露本地路径或造成文件损坏的问题，请不要在公开 Issue 中披露细节。请通过仓库的 **Security → Report a vulnerability** 私下提交报告。

普通兼容性问题和功能缺陷可以使用公开 Issue，并请先删除日志中的组织名、用户名、文件名和其他敏感信息。

## 支持范围

安全更新优先覆盖最新 Release。旧版本只保留用于复现和历史比较，不承诺继续修复。
